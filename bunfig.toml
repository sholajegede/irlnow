[install]
saveTextLockfile = true
# Supply-chain guard: skip package versions published less than 24 hours ago,
# which is the window in which most compromised releases are caught and pulled.
minimumReleaseAge = 86400
