# Copy to .env and fill in. Only VITE_-prefixed variables reach the browser —
# treat every one of them as public. Never put a secret in a VITE_ variable.

# Public origin this deployment is served from, no trailing slash.
# Used for share links, QR codes, .ics files and Open Graph tags.
VITE_APP_ORIGIN=http://localhost:8080
