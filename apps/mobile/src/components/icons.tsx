/**
 * Icons.
 *
 * The web app uses lucide-react, which renders SVG through the DOM. The
 * native equivalent is `lucide-react-native`, drawing through react-native-svg.
 * Re-exported through one module so a swap is a single edit, and so screens
 * import icons from the same place regardless of platform.
 */
export {
  ArrowLeft,
  ArrowRight,
  Bookmark,
  Calendar,
  CalendarCheck,
  Camera,
  Check,
  ChevronRight,
  Clock,
  Compass,
  Flame,
  Heart,
  Image as ImageIcon,
  MapPin,
  MessageCircle,
  Plus,
  QrCode,
  Search,
  Settings,
  Share2,
  Sparkles,
  Ticket,
  Users,
  UserRound,
  X,
} from "lucide-react-native";
