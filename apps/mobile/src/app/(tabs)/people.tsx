import { PlaceholderScreen } from "@/components/PlaceholderScreen";

/**
 * Not built yet.
 *
 * Rendered as an explicit "not built" state rather than a convincing shell,
 * so nobody demos this believing it works. See docs/STATUS.md.
 */
export default function PeopleScreen() {
  return <PlaceholderScreen title="People" />;
}
