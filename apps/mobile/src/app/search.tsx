import { PlaceholderScreen } from "@/components/PlaceholderScreen";

/**
 * Search.
 *
 * Pushed from the end of the feed, for someone who knows what they are
 * looking for rather than browsing. Not built yet — the route exists so the
 * end card's second action goes somewhere real.
 */
export default function SearchScreen() {
  return <PlaceholderScreen title="Search" />;
}
